# ds4viz 测试环境配置
# 用于开发和自动化测试

server:
  host: "127.0.0.1"
  port: 20000

database:
  host: "localhost"
  port: 5432
  database: "ds4viz_test"
  username: "ds4viz_test"
  password: "test_pwd_123"

security:
  access_token_expire_hours: 1
  bcrypt_rounds: 4
  jwt_secret: "test-jwt-secret-do-not-use-in-production"
  jwt_algorithm: "HS256"

sandbox:
  timeout_seconds: 10
  max_memory_mb: 256
  temp_dir: "/tmp/ds4viz_test/exec"

library:
  python_path: ""
  c_ds4viz_header_path: "../../../library/c/ds4viz.h"