# `ds4viz`: PHP编码规范

在编写PHP代码时，你需要遵循以下编码规范：

**语言级别:** `PHP 8.2+`  
**类型注解:** 使用严格类型声明(`declare(strict_types=1)`)，所有参数、返回值、属性都必须有类型声明。善用联合类型(`string|int`)、交集类型、nullable类型(`?string`)、mixed类型等PHP 8.0+特性  
**编码规范:** 严格遵循PSR-1、PSR-12和PSR-4标准。代码需要在PHP_CodeSniffer和PHPStan(level 9)最严格模式下无Warning。采用现代PHP写法，包括：使用constructor property promotion、named arguments、match表达式、null safe operator(`?->`)、nullsafe coalescing(`??`)、arrow functions、attributes、enum、readonly properties、fibers等PHP 8.0+新特性。善用命名空间、trait、接口、抽象类、匿名类等面向对象特性  
**PSR-4自动加载:** 命名空间必须遵循PSR-4规范，类名与文件名一致，目录结构与命名空间对应  
**文档注释:** 在文件开头、任何的方法、类、接口、trait、enum上都要有遵循PHPDoc标准的文档注释。代码即文档的编码规范：代码需要尽可能的自描述、易懂，减少非PHPDoc之外的注释内容。

## 命名约定

- **PascalCase**: 类名、接口名、trait名、enum名 (如 `DataProcessor`, `UserInterface`)
- **camelCase**: 方法名、函数名、属性名、变量名 (如 `getUserName`, `totalCount`)
- **UPPER_CASE**: 常量、enum case (如 `MAX_SIZE`, `DEFAULT_TIMEOUT`)
- **命名空间**: 遵循PSR-4，如 `Ds4viz\Utils\DataProcessor`

## 文件结构

每个PHP文件必须按以下顺序组织：

1. 开始标签 `<?php`
2. 严格类型声明 `declare(strict_types=1);`
3. 文件级PHPDoc注释
4. 命名空间声明
5. use语句（按字母顺序）
6. 类/接口/trait/enum定义

## PHPDoc注释规范

### 文件开头

```php
<?php

declare(strict_types=1);

/**
 * 文件功能描述，使用中文
 * 多行描述可以继续写在这里
 *
 * @author WaterRun
 * @file src/Utils/DataProcessor.php
 * @package Ds4viz\Utils
 * @since 2025-12-23
 * @version 1.0.0
 */

namespace Ds4viz\Utils;
```

### 类

```php
/**
 * 类描述，中文
 * 详细说明类的用途和功能
 *
 * @package Ds4viz\Utils
 * @author WaterRun
 * @since 2025-12-23
 */
class DataProcessor
{
    // 类内容
}
```

### 接口

```php
/**
 * 接口描述，中文
 *
 * @package Ds4viz\Contracts
 * @author WaterRun
 */
interface ProcessorInterface
{
    // 接口方法
}
```

### Trait

```php
/**
 * Trait描述，中文
 *
 * @package Ds4viz\Traits
 * @author WaterRun
 */
trait LoggableTrait
{
    // Trait内容
}
```

### Enum

```php
/**
 * 枚举描述，中文
 *
 * @package Ds4viz\Enums
 * @author WaterRun
 */
enum ProcessingStatus: string
{
    /**
     * 待处理状态
     */
    case PENDING = 'pending';
    
    /**
     * 处理中状态
     */
    case PROCESSING = 'processing';
    
    /**
     * 已完成状态
     */
    case COMPLETED = 'completed';
    
    /**
     * 失败状态
     */
    case FAILED = 'failed';
}
```

### 属性

```php
/**
 * 属性描述，中文
 *
 * @var string
 */
private string $connectionString;

/**
 * 只读属性描述
 *
 * @var int
 */
private readonly int $maxRetries;
```

### 常量

```php
/**
 * 默认批处理大小
 *
 * @var int
 */
private const DEFAULT_BATCH_SIZE = 100;

/**
 * 最大超时时间（秒）
 *
 * @var int
 */
public const MAX_TIMEOUT = 300;
```

### 方法和函数

```php
/**
 * 方法功能描述，使用中文
 * 可以有更详细的说明
 *
 * @param string $paramName 参数说明
 * @param int|null $optionalParam 可选参数说明
 * @return array<int, mixed> 返回值说明
 * @throws \InvalidArgumentException 当参数无效时抛出
 * @throws \RuntimeException 当处理失败时抛出
 * @see AnotherClass::relatedMethod() 相关方法引用
 * @link https://example.com/docs 相关文档链接
 * @deprecated 自版本2.0起弃用，使用newMethod()代替
 * @since 1.0.0
 */
public function methodName(string $paramName, ?int $optionalParam = null): array
{
    // 实现
}
```

### 构造函数（使用Property Promotion）

```php
/**
 * 构造函数描述
 *
 * @param string $connectionString 数据源连接字符串
 * @param int $batchSize 批处理大小，默认100
 * @param array<string, mixed> $options 额外配置选项
 * @throws \InvalidArgumentException 当连接字符串为空时抛出
 */
public function __construct(
    private readonly string $connectionString,
    private int $batchSize = 100,
    private array $options = [],
) {
    if (empty($this->connectionString)) {
        throw new \InvalidArgumentException('连接字符串不能为空');
    }
}
```

### 泛型注解（使用PHPStan/Psalm语法）

```php
/**
 * 泛型方法描述
 *
 * @template T
 * @param array<T> $items 输入数组
 * @param callable(T): bool $predicate 过滤条件
 * @return array<T> 过滤后的数组
 */
public function filter(array $items, callable $predicate): array
{
    return array_filter($items, $predicate);
}

/**
 * 泛型类描述
 *
 * @template TKey of array-key
 * @template TValue
 */
class Collection
{
    /**
     * @param array<TKey, TValue> $items
     */
    public function __construct(
        private array $items = []
    ) {}
}
```

## 最佳实践

1. **严格类型**: 始终使用 `declare(strict_types=1)`
2. **PSR-4自动加载**: 命名空间与目录结构严格对应
3. **不可变性**: 优先使用 `readonly` 属性和 `final` 类
4. **命名参数**: 在参数较多时使用命名参数提高可读性
5. **Match表达式**: 使用 `match` 替代 `switch`，更安全严格
6. **Null安全**: 使用 `?->` 和 `??` 处理可空值
7. **箭头函数**: 简单闭包使用 `fn()` 语法
8. **属性提升**: 构造函数参数直接声明为属性
9. **枚举优先**: 使用 `enum` 替代类常量组
10. **异常处理**: 使用类型化异常，遵循PSR异常接口
